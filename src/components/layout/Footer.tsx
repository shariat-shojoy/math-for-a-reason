import "./Footer.css";

export function Footer() {
  return (
    <footer className="site-footer">
      <p className="mono">
        math for a reason — built to answer "okay, but where would I actually use this?"
      </p>
    </footer>
  );
}
